/**
 * 探测器
 */

class Detector {

     constructor (rules) {
         this._rules = rules;
     }

     parse(ua) {
         ua = (ua || '').toLowerCase();

         const d = {};


         return d;
     }
 }